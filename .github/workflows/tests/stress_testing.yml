name: Stress Testing

on:
  pull_request:
    branches: [dev]
    types: [labeled]
  workflow_call:
  workflow_dispatch:

jobs:
  run:
    name: Run
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}
