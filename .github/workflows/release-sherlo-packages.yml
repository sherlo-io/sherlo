name: Release Sherlo Packages

on:
  workflow_dispatch:
    inputs:
      NEW_VERSION_NUMBER:
        description: 'New version number (e.g., 1.0.1)'
        required: false
      BUMP_REQUIRED_SDK_VERSION:
        description: 'SDK contains breaking changes'
        required: false
        default: false
        type: boolean
      IS_ALPHA:
        description: 'Is alpha release'
        required: false
        default: false
        type: boolean

permissions:
  id-token: write # Required for OIDC trusted publishing
  contents: write # Required for commits and tagging

jobs:
  release-to-prod:
    runs-on: ubuntu-latest

    env:
      SLACK_WEBHOOK_URL: ${{secrets.SLACK_WEBHOOK_URL}}
      NEW_VERSION_NUMBER: ${{ github.event.inputs.NEW_VERSION_NUMBER }}
    concurrency: release-to-prod
    steps:
      - name: Create GH App Token
        id: github_app_token
        uses: tibdex/github-app-token@v2
        with:
          app_id: ${{ secrets.SHERLO_BOT_APP_ID }}
          private_key: ${{ secrets.SHERLO_BOT_PRIVATE_KEY }}

      - uses: actions/checkout@v4
        with:
          token: ${{ steps.github_app_token.outputs.token }}
          ref: ${{ github.ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Download Sherlo packages
        run: ./scripts/init-env.sh
        env:
          CLIENT_STAGE: prod
          PACKAGE_TOKEN: ${{ secrets.PACKAGE_TOKEN }}

      - name: Install dependencies
        run: yarn

      - name: Bump Version
        run: npx lerna version $NEW_VERSION_NUMBER --yes --no-git-tag-version --no-push

      - name: Save version number to native dependencies
        run: |
          echo "{\"version\": \"$NEW_VERSION_NUMBER\"}" > packages/react-native-storybook/android/src/main/assets/sherlo.json
          echo "{\"version\": \"$NEW_VERSION_NUMBER\"}" > packages/react-native-storybook/ios/Resources/assets/sherlo.json

      - name: Bump minimum required native dependencies version
        if: ${{ github.event.inputs.BUMP_REQUIRED_SDK_VERSION == 'true' }}
        run: |
          jq --arg v "$NEW_VERSION_NUMBER" '.REQUIRED_MIN_NATIVE_VERSION = $v' packages/react-native-storybook/src/sdk-compatibility.json > /tmp/sdk-compat.json && mv /tmp/sdk-compat.json packages/react-native-storybook/src/sdk-compatibility.json

      - name: Update JS module version in sdk-compatibility.json
        run: |
          jq --arg v "$NEW_VERSION_NUMBER" '.JS_MODULE_VERSION = $v' packages/react-native-storybook/src/sdk-compatibility.json > /tmp/sdk-compat.json && mv /tmp/sdk-compat.json packages/react-native-storybook/src/sdk-compatibility.json

      - name: Build packages
        run: yarn build

      - name: Update @sherlo/react-native-storybook version in examples
        run: |
          for dir in examples/standard examples/eas-update examples/eas-cloud-build; do
            cd $dir
            jq --arg version "$NEW_VERSION_NUMBER" '.dependencies["@sherlo/react-native-storybook"] = "^" + $version' package.json > package.json.tmp
            mv package.json.tmp package.json
            cd ../..
          done

      - name: Commit and push version updates
        uses: EndBug/add-and-commit@v7
        with:
          author_name: Sherlo Bot
          author_email: admin@sherlo.io
          message: Version updates and builds for ${{ github.event.inputs.NEW_VERSION_NUMBER }}
          push: true
          branch: ${{ github.ref_name }}

      - name: Publish to npm
        run: npx lerna publish from-package --no-private --yes --dist-tag ${{ github.event.inputs.IS_ALPHA == 'true' && 'alpha' || 'latest' }}

      - name: Tag Commit
        if: ${{ github.event.inputs.IS_ALPHA != 'true' }}
        run: |
          git tag ${{ github.event.inputs.NEW_VERSION_NUMBER }}
          git push origin ${{ github.event.inputs.NEW_VERSION_NUMBER }}

      # ────────────────────────────────────────────────────────────────────────
      # THE ACTION RELEASE REF - what `uses: sherlo-io/sherlo@v2` runs.
      #
      # `uses:` checks this repository out at the ref and runs what is committed
      # there, so the ref customers pin has to carry a built CLI: with one, a job
      # that installed no dependencies still works; without one, the action needs
      # the project's own installed CLI (which is still preferred when present).
      #
      # `v2` is the moving major tag customers pin (README's Quick Start). It is
      # force-updated to each release's commit; `action-release/v2` is the branch
      # holding the same commit, so the history of the ref is readable.
      # ────────────────────────────────────────────────────────────────────────
      - name: Push the action release ref
        if: ${{ github.event.inputs.IS_ALPHA != 'true' }}
        run: |
          VERSION=$(node -p "require('./lerna.json').version")
          MAJOR_TAG="v${VERSION%%.*}"
          BRANCH="action-release/${MAJOR_TAG}"

          ./scripts/build-carried-cli.sh "$VERSION"

          git config user.name "Sherlo Bot"
          git config user.email "admin@sherlo.io"

          git checkout -B "$BRANCH"
          git add -f actions/carried-cli
          git commit -m "Action release: sherlo@${VERSION}"

          git push --force origin "$BRANCH"
          git tag -f "$MAJOR_TAG"
          git push --force origin "$MAJOR_TAG"

          ACTION_SHA=$(git rev-parse HEAD)

          # Back to the branch this release is being cut from: every step after this
          # one still commits and reads there.
          git checkout -

          {
            echo "### Action release ref"
            echo ""
            echo "- tag: \`$MAJOR_TAG\` (what \`uses: ${{ github.repository }}@$MAJOR_TAG\` resolves to)"
            echo "- branch: \`$BRANCH\`"
            echo "- sha: \`$ACTION_SHA\`"
          } >> $GITHUB_STEP_SUMMARY

      - name: Create Release
        if: ${{ github.event.inputs.IS_ALPHA != 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.NEW_VERSION_NUMBER }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Wait for npm registry propagation
        run: |
          TIMEOUT=600
          INTERVAL=15
          ELAPSED=0
          echo "Waiting for sherlo@$NEW_VERSION_NUMBER and @sherlo/react-native-storybook@$NEW_VERSION_NUMBER on npm..."
          while true; do
            SHERLO_OK=$(npm view sherlo@$NEW_VERSION_NUMBER version 2>/dev/null)
            SDK_OK=$(npm view @sherlo/react-native-storybook@$NEW_VERSION_NUMBER version 2>/dev/null)
            if [ -n "$SHERLO_OK" ] && [ -n "$SDK_OK" ]; then
              echo "Both packages available: sherlo@$SHERLO_OK, @sherlo/react-native-storybook@$SDK_OK"
              break
            fi
            if [ $ELAPSED -ge $TIMEOUT ]; then
              echo "ERROR: Timed out after ${TIMEOUT}s waiting for npm propagation."
              echo "  sherlo@$NEW_VERSION_NUMBER: ${SHERLO_OK:-not found}"
              echo "  @sherlo/react-native-storybook@$NEW_VERSION_NUMBER: ${SDK_OK:-not found}"
              exit 1
            fi
            echo "Not yet available (${ELAPSED}s elapsed). Retrying in ${INTERVAL}s..."
            sleep $INTERVAL
            ELAPSED=$((ELAPSED + INTERVAL))
          done
          
      - name: Update lock files in examples and test directories
        run: |
          for dir in examples/standard examples/eas-update examples/eas-cloud-build; do
            cd $dir
            yarn install --no-immutable
            cd ../..
          done
          cd testing/expo && yarn install --no-immutable
          cd ../react-native && yarn install --no-immutable

      - name: Commit and push updated lock files
        uses: EndBug/add-and-commit@v7
        with:
          author_name: Sherlo Bot
          author_email: admin@sherlo.io
          message: Update lock files for ${{ github.event.inputs.NEW_VERSION_NUMBER }}
          push: true
          branch: ${{ github.ref_name }}

      - name: notify failure on Slack
        if: failure()
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          fields: repo,commit,author,action,eventName,ref,workflow,job,took
