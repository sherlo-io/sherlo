node_modules/

.expo/
.next/
.vscode/
.idea/
.yarn/*
!.yarn/releases
.local-data/

dist/
# The CLI copy the GitHub Action carries (actions/carried-cli) is built by
# scripts/build-carried-cli.sh and committed onto release refs with `git add -f`.
# On every other ref it is absent, and the rules above keep a local build untracked.
builds/
*.apk
*.app
*.tar.gz

sherlo-lib/
!testing/expo/sherlo-lib/
testing/expo/sherlo-lib/*
!testing/expo/sherlo-lib/react-native-storybook.tgz
!testing/react-native/sherlo-lib/
testing/react-native/sherlo-lib/*
!testing/react-native/sherlo-lib/react-native-storybook.tgz
.env
sherlo.config.json
!examples/*/sherlo.config.json

*storybook.log
storybook-static

.cache/
