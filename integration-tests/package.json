{
  "name": "@sherlo/integration-tests",
  "private": true,
  "scripts": {
    "sync-sdk": "node scripts/sync-sdk.mjs",
    "prebuild": "tsx scripts/prebuild.ts",
    "test": "vitest run",
    "test:unit": "vitest run harness",
    "test:integration": "PLATFORM=android vitest run suites/ && PLATFORM=ios vitest run suites/",
    "test:watch": "vitest",
    "manual-apply": "tsx scripts/manual-apply.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.3.3",
    "vitest": "^3.0.0"
  }
}
