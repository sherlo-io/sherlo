/**
 * THE POSE CATALOGUE LAW (sherlo / Drawing for a plan). A shell the plan sign-off demanded: the
 * name is the rule on the page, and the body is the build task's (cli-pose-command) to write.
 */
import { describe, it } from 'vitest';

describe('the pose catalogue under packages/cli/poses', () => {
  it.todo('every catalog pose renders and its rendered bytes are committed beside it');
});
