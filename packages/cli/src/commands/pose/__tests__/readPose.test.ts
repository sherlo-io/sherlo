/**
 * THE POSE READER (sherlo / Drawing for a plan). A shell the plan sign-off demanded: the name
 * is the rule on the page, and the body is the build task's (cli-pose-command) to write.
 */
import { describe, it } from 'vitest';

describe('reading a CommandPose', () => {
  it.todo('a pose with a missing field, an unknown key or a value of the wrong type is refused naming every problem at once');
});
