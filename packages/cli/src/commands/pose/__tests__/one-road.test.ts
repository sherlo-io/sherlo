/**
 * ONE ROAD (sherlo / Drawing for a plan). A shell the plan sign-off demanded: the body is the
 * build task's (cli-pose-delete-old-roads) to write, once the tester has converted.
 */
import { describe, it } from 'vitest';

describe('sherlo pose is the one road to a planned transcript', () => {
  it.todo('no flag, script or document names a transcript road other than sherlo pose');
});
