/**
 * `sherlo pose` (sherlo / Drawing for a plan). Shells the plan sign-off demanded: each name is a
 * rule on the page, and the bodies are the build task's (cli-pose-command) to write.
 */
import { describe, it } from 'vitest';

describe('sherlo pose runs one command against a declared world', () => {
  it.todo('a call the command makes that the pose did not script is refused, and the screen so far is printed with it');
  it.todo('the bytes sherlo pose prints for a scenario equal the bytes a real terminal records for the same scenario');
});
