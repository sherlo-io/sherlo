/**
 * THE SIXTH SEAM - `sherlo init` posed. The command installs a package, reports its progress to the
 * server and waits for a key; a pose answers the install and the key from `workstation`, the
 * reports from `api`, and the screen is the shipped tool's own. Shells written at plan (epic
 * beats-road-sagas); the task that lands the seam fills them.
 */
import { describe, it } from 'vitest';

describe('init runs against a pose: the install, the reports to the server and the key press are answered by the pose, and the screen is the shipped tool\'s own', () => {
  it.todo('a first setup on a stock project prints the files it created and changed, from a pose that says Enter was pressed');
  it.todo('a second setup on a project already set up prints Already updated and writes nothing');
  it.todo('a pose with no workstation is a terminal nobody sits at: the prompt is refused and the screen ends with Setup cancelled');
  it.todo('an install the pose did not answer is a refusal of the pose, never a real package manager run');
});
