/**
 * A BARE PUSH AGAINST A POSE - the staged gate answered by the pose, no binary read, and the screen
 * the shipped tool's own. Written as a skeleton in plan (epic legacy-road-closed, task
 * push-pose-staged-and-deadline); the worker fills the bodies and never renames a case.
 */
import { describe, it } from 'vitest';

describe('a bare push is posable', () => {
  it('a bare push runs against a pose: the staged gate is answered by the pose, no binary is read, and the screen is the shipped tool\'s own', () => {});
  it('a staged gate that answers registered prints the borrowed base and opens a build', () => {});
  it('a staged gate that answers unregistered prints native-needed=true and exits with the native-needed code', () => {});
  it('a dry run against a pose prints native-needed and the base fingerprint and opens nothing', () => {});
});
